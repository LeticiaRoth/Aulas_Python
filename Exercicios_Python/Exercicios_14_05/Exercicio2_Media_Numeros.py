numeros = [2, 5, 7, 9]

for numero in numeros:
    soma = sum(numeros) #Somo todos os números da lista
    media = soma /len(numeros) #Divido a soma pelo tamanho da lista


print(media) #Se colocar dentro do looping, vai repetir várias vezes, fora dele mostra apenas uma, o resultado

